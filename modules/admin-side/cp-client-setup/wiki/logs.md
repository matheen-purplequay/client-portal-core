# Logs

## 2026-07-01
- Ran `/wiki init`. Explored `source/` (actual project root for this module — note repo root has stray deleted-file entries in git status; real code lives in `source/src`). Surveyed app bootstrap (Web Component widget, Shadow DOM, `vite-plugin-css-injected-by-js`), the three-backend service layer, Zustand + plain-module dual state pattern, all `pages/clients/*` tab components, and `types/index.ts`. Created `wiki/architecture.md`, `wiki/issues.md` (logged the store/stores inconsistency), `wiki/index.md`, and root `CLAUDE.md`.
